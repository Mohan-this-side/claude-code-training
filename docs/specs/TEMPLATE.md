# SPEC · <ticket id> — <short title>

> Written before any code. Generated with `/spec`, then edited by a human.
> Load it as context when you build: `@docs/specs/<this-file>.md`

**Ticket:** [<ticket id>](../tickets/<ticket-id>.md)
**Author:** <you>
**Status:** draft | reviewed | building | done

## Problem

Who is stuck, what it costs them, and why now. Two or three sentences, in the reporter's terms rather than in technical ones.

## Current state

What the code does today. Every claim carries a file path.

- `src/...` — what lives here and what it currently does
- `src/...` — the helper that already exists for this
- Anything in the ticket that turns out not to match the code

## Domain rules

The constraints that must hold, quoted rather than paraphrased.

| Rule | Source | What breaks if ignored |
| --- | --- | --- |
| Money is integer minor units | `CLAUDE.md` | Cents drift on every total |
| | | |

## Approach

One paragraph on the shape of the change.

**Considered and rejected:** the other option, and the reason it lost. If nothing was rejected, the problem was not thought about hard enough.

## File map

| File | Add or change | Why |
| --- | --- | --- |
| | | |

## Plan

Sequenced so each step ends somewhere verifiable.

1. **<step>** — done when: <the observable result>
2. **<step>** — done when: <the observable result>
3. **<step>** — done when: <the observable result>

## Verification

How each acceptance criterion gets proven.

| Acceptance criterion | How it is proven |
| --- | --- |
| | |

## Risks

- <what could go wrong, and what you will do about it>

## Out of scope

- <what you are deliberately not building, and where it lives instead>

## Open questions

- <what you genuinely do not know yet>
